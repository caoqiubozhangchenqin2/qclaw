# 2026-04-25

## GitHub Push 诊断与修复
- **问题**：PowerShell 执行 `git push` 报 exit code 1，看似失败
- **根因**：git 把正常提示信息（如 `To https://...`、`Everything up-to-date`）写到 stderr，PowerShell 误判为错误。Push 实际一直成功。
- **认证**：`wincred` 凭证管理器已配置，GitHub 账号 `caoqiubozhangchenqin2` 凭据已存储，认证正常。
- **修复**：清理了嵌套的 `memory/memory/` 目录问题，成功推送 `96e6ec0`

## 每日自动备份定时任务 - 已完成
- 用 `openclaw cron add` CLI 创建任务（CLI因QQBOT警告刷屏被kill，但任务实际已创建成功）
- 问题：创建了3个重复的21:00备份任务，且缺少accountId
- 解决：直接编辑 `C:\Users\Administrator\.qclaw\cron\jobs.json`，删除3个重复任务，将原始任务从16:00改为21:00
- 最终配置：每日备份 21:00 + 周五观赛指南 12:00
- 教训：`openclaw cron add` CLI在当前环境不稳定（QQBOT警告导致超时kill），但任务会写入；可先CLI创建再手动编辑jobs.json清理
